package com.kodilla.stream.array;

public class ArrayAVG implements ArrayOperations {
    public double getAvg(int[] numbers) {
        double average = ArrayOperations.getAverage(numbers);
        return average;
    }
}
